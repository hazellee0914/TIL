/**
 * 숫자열인 입력데이터 2개를 받아 나누기 기능을 하는 함수를 만들어야 합니다.
 */

function divide(a, b) {
  console.log(a / b);
  return a / b;
}

divide(10, 2);

const divide2 = function (a, b) {
  console.log(a / b);
};

divide2(10, 2);

const divide3 = (a, b) => {
  console.log(a / b);
};

divide3(10, 2);
